#!/bin/bash

# -----------------------------------------------------------------------------
# APM PACKAGE NAMES
#
# Lists all installed Atom package names without version numbers. Useful to
# create a file which can be used to reinstall atom packages using apm on
# another machine.
#
# Usage: apm-package-names > filename
# With apm: apm install --packages-file <filename>
#
# Acknowledgements
# http://evanhahn.com/atom-apm-install-list/
# -----------------------------------------------------------------------------

apm list --installed --bare | grep '^[^@]\+' -o
