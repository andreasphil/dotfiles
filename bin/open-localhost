#!/bin/bash

# -----------------------------------------------------------------------------
# OPEN LOCALHOST
#
# Launches Safari with the current IP and optional port. Useful if we want to
# access a locally running server from a mobile device using Handoff.
#
# Usage: open-localhost [<port>]
# -----------------------------------------------------------------------------

if [[ "$1" == "" ]]
then
  port="8000"
else
  port="$1"
fi

open /Applications/Safari.app "http://$(myip):$port"
